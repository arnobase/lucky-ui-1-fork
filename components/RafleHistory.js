import React from "react";
const style = {
    rafleHistory: `text-xl font-semibold flex items-center justify-center`,
};

const RafleHistory = (props) => {
  return <div className={style.rafleHistory}>
    <div>...</div>
  </div>;
};
export default RafleHistory;